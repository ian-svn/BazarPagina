# Database (MySQL)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=sistema_bazar
DB_PORT=3306

# JWT
JWT_SECRET=cambiar_por_un_secreto_seguro_en_produccion

# Server
PORT=5000
NODE_ENV=development

# Frontend URL (para CORS y redirects)
CLIENT_URL=http://localhost:3000
